
<div class="card mt-3">
  <div class="card-header">
    <h1>Editing Trip</h1>
  </div>
  
  <div class="card-body">
    <%= render 'form', product: @product %>
  </div>
  <div class="card-footer">
    <%= link_to 'Show', @product %> |
    <%# I add below path in case when user in edit page, click back button will not go to listing page, I want them can go to the exactly product page %>
    <%= link_to 'Back', product_path(params[:id]) %>
  </div>
